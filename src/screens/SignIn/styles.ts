import { StyleSheet } from "react-native";


export const stylesSignIn = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#659',
    alignItems: 'center',
    justifyContent: 'center',
  },
});